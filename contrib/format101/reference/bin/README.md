# Reference binaries (format_101)

These executables are used as a compatibility reference for the TurboWin+ format_101 workflow:

- `teste_hc_TW*` (compression/encoding)
- `MAWSbin_TW*` (decompression/decoding / validation)

SHA256 checksums are listed in `SHA256SUMS`.
